name: open_file_example
description: Demonstrates how to use the open_file_safe plugin.

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons:
dev_dependencies:
  flutter_test:
    sdk: flutter

  open_file_safe:
    path: ../

flutter:
  uses-material-design: true
